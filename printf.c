#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void){
    char texto[] = "\"Programming is like building a multilingual puzzle \n";
    puts(texto);
    return (0);
}